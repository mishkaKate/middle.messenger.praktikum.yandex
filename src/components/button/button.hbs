<button class="button button-role-{{role}}">
    <a class="button-a" href={{href}}>
        {{#if icon-source}}
        {{> icon icon-source=icon-source}}
        {{/if}}
        {{text}}
    </a>
</button>
