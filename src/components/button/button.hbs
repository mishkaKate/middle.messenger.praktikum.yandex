<button class="button button_{{role}}">
    <a class="button__a" href={{href}}>
        {{#if icon-source}}
        {{> icon icon-source=icon-source}}
        {{/if}}
        {{text}}
    </a>
</button>